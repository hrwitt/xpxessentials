package com.example.xpxessentials

import com.squareup.okhttp.internal.framed.Variant

class DataHelper {

    fun retrieveCourseDetails(courseName: String):Array<String>{

        val courseDetailArray = arrayOf<String>()

        //TODO: Will populate the array with information drawn from firebase by a query

        return courseDetailArray
    }
}